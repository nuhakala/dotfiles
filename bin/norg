#!/bin/bash

muistiinpanot=~/muistiinpanot/
todos=~/gtd/

if [ $# -eq 0 ]; then
    cd ${muistiinpanot}
	nvim -c ":Neorg workspace notes"
    # nvim <<< "<esc>:Neorg workspace notes"
    # "lua vim.api.nvim_create_autocmd({':L'}, { command = 'Neorg workspace notes' })"
# ***** Kansiot *****
elif [ "$1" = "dir" ]; then
    cd ${muistiinpanot}
	nvim -c ":Neorg workspace notes" -c ":edit $2/index.norg"

# ***** Neorg indexit *****
elif [ "$1" = "hissa" ]; then
    cd ${muistiinpanot}
    nvim -c ":Neorg workspace notes" -c ":edit 2024-03-06--historia_index.norg"
elif [ "$1" = "ait" ]; then
    cd ${muistiinpanot}
    nvim -c ":Neorg workspace notes" -c ":edit 2024-04-12--internet_technology_index.norg"
elif [ "$1" = "agt" ]; then
    cd ${muistiinpanot}
    nvim -c ":Neorg workspace notes" -c ":edit 2024-04-11--algorithmic_game_theory_index.norg"
fi
